root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/server ./cmd/server"
  bin = "tmp/server"
  full_bin = "tmp/server"
  delay = 1000
  include_ext = ["go"]
  exclude_dir = ["bin", "docs", "tmp", "vendor"]
  pre_cmd = ["make swagger"]

[log]
  time = true
